defmodule FinancialSystem.Calculator do
  alias FinancialSystem.Money

  # def convert_currency(money, convertion_rate, new_num_code, new_alph_code, new_decimal_points) do
  #   number1 + number2
  # end
end
